# Base URL of the deployment platform backend (no trailing slash, no /api suffix).
# The /api prefix is appended automatically by the API client.
# - Local development: http://localhost:8000
# - Production (Vercel): set this to your backend's public URL, e.g. https://api.yourdomain.com
VITE_API_BASE_URL=http://localhost:8000
